import { Button } from "@freelbee/features/common";
import { ButtonStyleEnum } from "@freelbee/shared/ui-kit";

export const Tasks = () => {
  // rtkQueryErrorLogger();
  return (
    <div>
      Tasks
      <Button styleType={ButtonStyleEnum.BLACK}>Test</Button>
    </div>
  );
};
