export const enum SectionId {
    MAIN_BANNER = '_main',
    BENEFITS = '_benefits',
    WHO_NEED = '_who',
    STEPS = '_steps',
    TABLE = '_table',
    COUNTRIES = '_countries',
    DEVICES = '_devices',
    INSIDE = '_inside',
    PRICING = '_pricing',
    SECURITY = '_security',
    FAQ = '_faq',
    CONTACTS = '_contacts',
    QUESTIONS_FORM = '_form'
}